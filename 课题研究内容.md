(1)  小穗级定向目标检测
面向小麦穗部图像中“小目标密集、姿态多变、相互遮挡”的检测难点，构建并训练了基于 Ultralytics 框架的 YOLO11-OBB（Oriented Bounding Box）定向目标检测模型，实现对小穗实例的高精度识别与定位。与传统水平检测框相比，OBB 通过四角点参数化表达目标几何形态与方向信息，不仅显著降低倾斜目标场景下的框间冗余重叠与定位偏差，也为后续“茎-穗”骨架构建提供了高质量的端点先验与方向约束，形成从检测到结构化分析的关键数据入口。

(2)“茎-穗”拓扑骨架构建
为克服传统像素级细化方法在噪声干扰下易出现断裂、毛刺与拓扑漂移的问题，本文提出一种由检测几何先验驱动的“茎-穗”拓扑骨架重构方法。设图像中共检测到 $N$ 个小穗，第 $i$ 个小穗 OBB 的四角点为 $\mathbf{P}_i=\{\mathbf{p}_{i1},\mathbf{p}_{i2},\mathbf{p}_{i3},\mathbf{p}_{i4}\}$。首先由 OBB 对边中点确定小穗中轴方向向量 $\mathbf{u}_i$，并以中轴两端点中更接近茎部的一端作为基部节点 $\mathbf{b}_i$，形成节点集合 $\mathcal{B}=\{\mathbf{b}_i\}_{i=1}^{N}$。

为估计主茎全局方向，对 $\mathcal{B}$ 执行 PCA。记节点均值为 $\bar{\mathbf{b}}=\frac{1}{N}\sum_{i=1}^{N}\mathbf{b}_i$，协方差矩阵为
$$
\mathbf{\Sigma}=\frac{1}{N}\sum_{i=1}^{N}\left(\mathbf{b}_i-\bar{\mathbf{b}}\right)\left(\mathbf{b}_i-\bar{\mathbf{b}}\right)^\top.
$$
取最大特征值对应特征向量 $\mathbf{e}_1$ 作为主茎方向，并通过投影标量
$$
t_i=\mathbf{e}_1^\top\left(\mathbf{b}_i-\bar{\mathbf{b}}\right)
$$
完成节点沿茎向排序（记排序后为 $\mathbf{b}_{(1)},\dots,\mathbf{b}_{(N)}$）。

随后采用归一化累计弦长参数化建立连续茎曲线。定义
$$
s_1=0,\quad
s_i=\frac{\sum_{j=2}^{i}\left\|\mathbf{b}_{(j)}-\mathbf{b}_{(j-1)}\right\|_2}
{\sum_{j=2}^{N}\left\|\mathbf{b}_{(j)}-\mathbf{b}_{(j-1)}\right\|_2},\ i=2,\dots,N.
$$
在参数空间 $s\in[0,1]$ 上，求解加权三次平滑样条 $\mathbf{c}(s)=[x(s),y(s)]^\top$：
$$
\min_{\mathbf{c}}
\sum_{i=1}^{N}\omega_i\left\|\mathbf{c}(s_i)-\mathbf{b}_{(i)}\right\|_2^2
+\lambda\int_{0}^{1}\left\|\mathbf{c}^{\prime\prime}(s)\right\|_2^2\,\mathrm{d}s,
$$
其中 $\omega_i$ 为节点置信权重，$\lambda$ 为平滑系数。对 $[0,1]$ 均匀采样得到高密度主茎骨架点序列 $\{\mathbf{c}(s_k)\}_{k=1}^{K}$。

最后在每个节点位置计算切向量 $\boldsymbol{\tau}_i=\frac{\mathbf{c}'(s_i)}{\|\mathbf{c}'(s_i)\|_2}$，并与小穗中轴向量 $\mathbf{u}_i$ 做二维叉乘标量判别左右侧归属：
$$
\chi_i=\tau_{ix}u_{iy}-\tau_{iy}u_{ix},\quad
\sigma_i=
\begin{cases}
+1, & \chi_i>0,\\
-1, & \chi_i<0.
\end{cases}
$$
据此构建带方向与侧向标签的拓扑结构 $\mathcal{G}=(\mathcal{V},\mathcal{E})$，实现从离散 OBB 到连续生长结构的稳定映射，为后续整穗表型计算提供结构化几何基底。

(3)双视角表型特征提取
基于第(1)与第(2)部分输出，本文构建“局部器官-整体穗型”双视角表型特征体系。设像素到物理尺度换算系数为 $\kappa=\frac{D_{\mathrm{ref}}}{d_{\mathrm{ref}}}$（单位：mm/pixel），其中 $D_{\mathrm{ref}}$ 与 $d_{\mathrm{ref}}$ 分别为标定物真实尺寸与图像像素尺寸。

1) 小穗级局部特征。对第 $i$ 个小穗，定义长轴长度、短轴宽度、长宽比与着生角度为
$$
\ell_i=\max_{m=1,\dots,4}e_{im},\quad
w_i=\min_{m=1,\dots,4}e_{im},\quad
r_i=\frac{\ell_i}{w_i+\varepsilon},
$$
$$
\vartheta_i=\arccos\left(
\frac{|\mathbf{u}_i^\top\boldsymbol{\tau}_i|}
{\|\mathbf{u}_i\|_2\|\boldsymbol{\tau}_i\|_2+\varepsilon}
\right),
$$
其中 $e_{im}$ 为 OBB 第 $m$ 条边长，$\varepsilon$ 为数值稳定项。经尺度统一后有 $\tilde{\ell}_i=\kappa\ell_i,\ \tilde{w}_i=\kappa w_i$。为形成单株层面可比较的局部统计量，取均值聚合：
$$
\bar{\ell}=\frac{1}{N}\sum_{i=1}^{N}\tilde{\ell}_i,\quad
\bar{w}=\frac{1}{N}\sum_{i=1}^{N}\tilde{w}_i,\quad
\bar{r}=\frac{1}{N}\sum_{i=1}^{N}r_i,\quad
\bar{\vartheta}=\frac{1}{N}\sum_{i=1}^{N}\vartheta_i.
$$

2) 整穗级全局特征。基于主茎曲线 $\mathbf{c}(s)$ 与侧向标签 $\sigma_i$，定义
$$
L=\kappa\int_{0}^{1}\|\mathbf{c}'(s)\|_2\,\mathrm{d}s,\quad
N=\sum_{i=1}^{N}1,\quad
\rho=\frac{N}{L+\varepsilon},
$$
分别表示穗长、小穗总数与着生密度。设左、右两侧样本数为 $N_L=\sum\mathbf{1}(\sigma_i=-1),\ N_R=\sum\mathbf{1}(\sigma_i=+1)$，并令 $\mathbf{g}_i=[\tilde{\ell}_i,\tilde{w}_i,r_i,\vartheta_i]^\top$，则两侧形态均值为 $\boldsymbol{\mu}_L,\boldsymbol{\mu}_R$。定义数量差异与形态差异：
$$
\delta_N=\frac{|N_L-N_R|}{N+\varepsilon},\quad
\delta_M=\frac{\|\boldsymbol{\mu}_L-\boldsymbol{\mu}_R\|_1}
{\|\boldsymbol{\mu}_L\|_1+\|\boldsymbol{\mu}_R\|_1+\varepsilon}.
$$
对称度指数取
$$
S_{\mathrm{sym}}=1-\left[\beta\delta_N+(1-\beta)\delta_M\right],\quad
S_{\mathrm{sym}}\in[0,1].
$$
主轴重心偏移度定义为
$$
S_{\mathrm{cen}}=\frac{\left|\frac{1}{N}\sum_{i=1}^{N}s_i-0.5\right|}{0.5},
$$
其值越大表示结实区域越偏离穗轴中部。

最终，构建 9 维穗型特征向量
$$
\mathbf{f}=\left[\bar{\ell},\bar{w},\bar{r},\bar{\vartheta},L,N,\rho,S_{\mathrm{sym}},S_{\mathrm{cen}}\right]^\top\in\mathbb{R}^{9},
$$
实现由“小穗局部形态”到“整穗整体构型”的统一表征，为后续群体聚类与代表样本筛选提供可计算输入。

(4)批量样本穗型聚类分析
在获得批量样本的穗型特征向量后，引入无监督学习方法开展群体尺度的穗型聚类分析。首先，采用 Z-score 标准化（StandardScaler）消除不同物理量纲间的尺度差异，提升特征空间可比性；其次，利用主成分分析（PCA）完成初步降维与噪声抑制；随后，在 PCA 特征空间中采用基于 Ward 最小方差法的层次聚类算法（Agglomerative Clustering）挖掘样本群体间潜在的层级结构关系，并同步生成树状图（Dendrogram）；最后，基于同一 PCA 特征进一步使用 t-SNE 进行二维嵌入可视化，结合轮廓系数（Silhouette Score）评估聚类质量，并筛选各簇的代表性样本（Representative Image），输出直观的二维散点图。

(5)Web服务端系统开发
围绕算法工程化落地需求，构建了基于 Python Flask 的跨平台Web服务端系统，实现“小穗检测-骨架构建-表型计算-聚类分析-可视化输出”的全流程在线化。系统后端采用模块化分层架构，将模型推理、几何计算、统计分析与文件管理解耦封装为独立服务单元，并通过 RESTful API 对外提供单样本分析与批量任务处理能力；在任务执行层面，支持对上传图像进行队列化调度、进度追踪与异常回传，避免长耗时计算阻塞前端交互；在结果管理层面，统一生成结构化特征文件（CSV）与可视化交互画布（检测图、骨架图、聚类散点图、聚类树状图、对比雷达图/柱状图等），并按批次建立可追溯的目录索引，保障实验复现性与数据可审计性。得益于 Python 运行时与Web标准协议的天然兼容性，该服务端可在 Windows/Linux 等主流环境稳定部署，配合轻量化前端页面即可实现“浏览器即分析终端”的跨设备访问模式，为后续规模化田间表型数据处理与教学演示应用提供了可扩展的软件基础。